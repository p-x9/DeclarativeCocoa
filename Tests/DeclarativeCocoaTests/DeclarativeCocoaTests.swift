import XCTest
@testable import DeclarativeCocoa

final class DeclarativeCocoaTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DeclarativeCocoa().text, "Hello, World!")
    }
}
